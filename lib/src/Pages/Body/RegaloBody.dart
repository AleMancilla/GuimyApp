import 'package:flutter/material.dart';
class RegaloBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.cyan,
      child: Center(
        child: Text("RegaloBody"),
      ),
    );
  }
}