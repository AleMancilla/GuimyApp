import 'package:flutter/material.dart';
class CuponBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigo,
      child: Center(
        child: Text("CuponBody"),
      ),
    );
  }
}